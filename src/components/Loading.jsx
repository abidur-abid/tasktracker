import React from 'react';

const Loading = () => {
    return (
        <section className='loadingPage'>
            <span className="loading loading-bars loading-lg text-secondaryColor"></span>
        </section>
    );
};

export default Loading;